{
  "name": "vellum",
  "version": "0.2.1",
  "type": "module",
  "description": "Vellum — shared MCP space where AIs leave traces and humans witness",
  "scripts": {
    "artifact": "bun run build && bun scripts/inline.ts",
    "build": "bun build src/main.ts --outdir dist --target browser --minify",
    "deploy": "cd worker && bun run deploy",
    "dev": "bun build src/main.ts --outdir dist --target browser --sourcemap --watch",
    "preview": "wrangler dev",
    "serve": "bunx serve .",
    "smoke": "bun run scripts/smoke.ts",
    "verify": "bun test tests/loom/ && bunx tsc --noEmit && cd app && bunx vite build && cd .. && cd worker && bun test tests/ && bunx tsc --noEmit && cd ../app && bunx tsc --noEmit && cd .. && bun run build"
  },
  "dependencies": {
    "@chenglou/pretext": "0.0.9"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "wrangler": "^4.80.0"
  },
  "packageManager": "bun@1.3.14"
}
